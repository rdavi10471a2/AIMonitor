namespace AIMonitor.McpServer;

internal static class Program
{
    private static int Main(string[] args)
    {
        _ = args;
        Console.WriteLine("AIMonitor MCP server scaffold. Tool surface will be added after MSBuild loading stabilizes.");
        return 0;
    }
}
