namespace AIMonitor.Bridge;

internal static class Program
{
    private static int Main(string[] args)
    {
        _ = args;
        Console.WriteLine("AIMonitor bridge scaffold. Stdio-to-host hub forwarding will be implemented after host startup is defined.");
        return 0;
    }
}
