# Architecture Diagramming Eval Run - main 6f15f66

Agent: Wegener

Target: `C:\VSCodeProjects\AIMonitor-main-skill-eval`

Commit: `6f15f669669c1b2cd237e8a4773b686203d9d59a`

Mode: read-only

MCP visibility: no live AIMonitor MCP tools visible; used docs/source/tests as evidence.

## Output

### 1. Adapter Boundary

```text
Claude Code MCP
  -> AIMonitor.McpStdioBridge
  -> WinForms-owned MCP proxy hub
  -> AIMonitor.McpServer
  -> shared Workflow / Indexing / Data / MSBuild / Runtime services

Codex / shell
  -> AIMonitor.Cli
  -> same shared services

WinForms UI
  -> Solution Index views use Data/Indexing directly
  -> Monitor Status observes adapter telemetry
```

Key read: adapters should own names, transport, response shape, and telemetry; they should not own workflow truth. CLI vocabulary is `edit refresh`; MCP vocabulary is `refresh_file`.

### 2. Safe Edit / Review / Decision

```text
WATCHED SOURCE
  -> refresh/new creates monitor-owned Working candidate
  -> agent edits Working only
  -> stage snapshots immutable staged record + hashes
  -> launch-diff runs pre-merge validation copy build
       red -> human override required before WinMerge
  -> WinMerge review/save is the watched-source mutation point
  -> record-decision classifies by vote + staged/watched hashes
       accepted | accepted-normalized | rejected | dirty-unexpected
  -> accepted decisions rebuild solution index
  -> refresh-required before another edit to that file
```

Key read: `record-decision` verifies what the human saved; it does not copy staged bytes into watched source. Staged runtime files are evidence after staging.

### 3. MSBuild / Index / Razor Boundary

```text
MonitorSettings.WatchedSolutionPath
  -> MSBuildWorkspaceLoader
       project graph, documents, references, diagnostics
  -> MSBuildSolutionSnapshot
  -> SolutionIndexBuilder
  -> SolutionIndexStore / SQLite rows
       symbols, references, callers, relationships, stale counts
  -> Query services
  -> CLI JSON / MCP tools / WinForms index UI

Razor evidence:
  generated Razor C# span maps cleanly to user .razor source -> index as razor:* ref
  raw markup binding semantics not proven -> grep/build/smoke evidence only
  raw .razor Roslyn symbol tools -> explicit refusal / use text workflow
```

Key read: MSBuild owns project truth; C# is the first semantic provider, not the whole architecture. Razor support is conservative mapping evidence, not a Visual Studio-level binding model.

### 4. Planned Session / Gate Ambiguity

```text
PLAN       start session with filesPlanned=[A,B,C]
             |
EDIT       create Working A', B', C'
             |
GATE 1     Roslyn overlay semantic compile
           predictive; catches C# cross-file errors
           skips Razor markup, MSBuild, analyzers, source-gen
             |
REVIEW     stage + launch + WinMerge per file
             |
DECISION   record accepted/rejected per staged record
             |
GATE 2     full build / index refresh on real accepted tree
           authoritative for what actually landed
```

Key read: this two-gate model appears in planning docs, while the current single-file CLI/MCP `launch-diff` path still runs a full validation-copy build before WinMerge. Agents need that distinction labeled, because "validation" means different things across these flows.

Plan Board/current-task note: the agent did not find an implemented Plan Board/current-task surface in this main worktree. Searches for `plan current`, `get_current_task_context`, and current task context found adjacent planning/memory review docs, not active code or operator docs. The run recommends documenting that as "not visible on main" rather than diagramming it as shipped.

## Skill Critique

The architecture-diagramming skill helped most by forcing separation of actor, artifact, gate, and authority. That made the important ambiguity obvious: predictive overlay checks, full builds, hash checks, WinMerge saves, and index rebuilds are all different gates.

Missing instruction: the skill should explicitly require a "docs vs code freshness" pass. AIMonitor has branch/planned-session docs beside mainline code, and without labeling which evidence is implemented versus proposed, an agent could draw a confident but wrong architecture.

## Follow-Up Applied

The skill and Claude card were updated to require a freshness check that separates implemented code, published contracts, branch/proposal docs, and inference.
